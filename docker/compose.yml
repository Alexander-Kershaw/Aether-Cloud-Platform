name: acp

services:
  bootstrap:
    image: alpine:3.20.3
    command: ["sh", "-c", "echo 'ACP bootstrap container is running' && tail -f /dev/null"]
    networks: [acp-net]

networks:
  acp-net:
    name: acp-net